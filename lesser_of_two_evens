#defines a function with the name 'lesser_of_two_evens' with the parameters (a,b)
#this can be test with calling the function below the block using something like `lesser_than_two_evens(2,3)' 
#and 'lesser_than_two_evens(2,4)


def lesser_of_two_evens(a,b):
    if a%2 == 0 and b%2 == 0:
        return min(a,b)
    else:
        return max(a,b)
